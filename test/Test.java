import org.junit.*;
import static org.junit.Assert.*;

/*
 * Just a class shell so that it may be pushed to github
 * Possibly use JUnit to create the tests, though I'm not sure how JUnit behaves
 * without an IDE.
 */
class Test {

}
